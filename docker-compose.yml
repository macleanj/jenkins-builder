version: "3.7"

services:
  jenkins-builder:
    hostname: jenkins-builder
    container_name: jenkins-builder
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    # image: jmaclean/jenkins-builder:0.5.build
    image: r.j3ss.co/img
    security_opt:
      - apparmor:unconfined
      # - seccomp:unconfined
    volumes:
      - .:/app
    entrypoint:
      - tail
      - -f
      - /dev/null
